name: frontend
description: "École enrollment application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  
  # State Management
  provider: ^6.1.1
  
  # HTTP Client
  dio: ^5.4.0
  
  # Secure Storage for JWT
  flutter_secure_storage: ^9.0.0
  
  # Image Picker
  image_picker: ^1.0.7
  
  # Cached Network Images
  cached_network_image: ^3.3.1
  
  # Google Fonts
  google_fonts: ^6.1.0
  
  # Intl for date formatting
  intl: ^0.18.1
  
  # Charts
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
